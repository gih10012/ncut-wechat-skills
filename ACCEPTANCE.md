# v0 新对话验收

两套 skill 已安装在本机。请新开对话，直接使用下面的问题，无需复制本次开发历史、接口或抓包路径。账号默认 `me`；登录失效时应先尝试保存的会话，确需本人认证才交接。

| 测试问题 | 预期行为 |
| --- | --- |
| 查本周课表 | 直接执行已有课表命令；有效会话下两次 HTTP，不查前端源码 |
| 查我最近一个有成绩的学期的课程成绩 | 命中成绩能力，学期取自学校当前选项，显示学校原始成绩，不猜学号或自行算绩点 |
| 再查一次同一个学期的成绩 | 直接复用；正常响应不重新探索或改知识文件 |
| 查今晚18:00–20:30校本部的空教室 | 按当前日期核对整个时段，使用最新学校记录；登录失效与无空教室明确区分 |
| 当前能读取我的企业微信聊天吗？ | 准确说明企微原生读取尚未接通，不把学校网页、机器人会话或个人微信本地数据冒充企微收件箱 |

现有微信读取可另用“读取文件传输助手最近5条消息”做分流回归，应直接使用本机数据，不操作聊天窗口。

实现阶段已真实验证成绩发现、二次查询、课表和空教室；相关离线测试覆盖未验证能力分流、学期选择、原始成绩、空结果、错误响应以及登录/权限/网络错误停止。真实成绩空结果和权限错误未主动在本人账号上制造；这些分支仅有离线覆盖。

仍待本人确认的是新对话的选择、表达和使用体验。反馈时给出测试问题、实际行为和是否绕路即可；不要粘贴 Cookie、密钥、验证码或完整个人响应。

2026-09-17晚本人授权继续自动验收并推进v1。回归从已安装skill路径、以`/tmp`为工作目录执行：微信文件传输助手读取5条成功；企微路由正确返回未接通；知识文件未改变。学校课表、成绩和空教室当前返回HTTP503/TLS错误，未把这轮在线回归记为通过，也未反复要求本人登录。此前成功的实际学校查询证据仍保留，但不能替代当前可达性。

v1实际验收：本人扫码绑定ClawBot并发送测试文字，第三方SDK真实读到；真实MCP客户端完成会话、5条本机消息和ClawBot增量读取。尚未证明原生发送、OneBot独立登录或企微消息读取；这些不算本轮成功范围。

## 2026-09-18自动续接后的实际复查

仍从 `/tmp` 调用已安装skill，不使用旧抓包路径。教务起初返回HTTP200的 `AUTH_REQUIRED`；保存的SSO会话仍有效，处理教务过期提示后完成本科换票，真实课表接口返回 `LOGIN_READY`，专用浏览器已关闭，无需本人扫码。

| 测试问题 | 实际表现 |
| --- | --- |
| 查本周课表 | 成功，返回当前学期和当周非空课程安排 |
| 查我最近一个有成绩的学期的课程成绩 | 成功，返回非空课程成绩及学校当前学期选项 |
| 再查一次同一个学期的成绩 | 使用第一次返回的学期ID直接查询，学期与课程记录一致，知识文件未变化 |
| 查今晚18:00–20:30校本部的空教室 | 按2026-09-18实际查询成功，检查两个相交节次，返回有界教室列表 |
| 读取文件传输助手最近5条消息 | 成功，直接读取现有Linux本地数据，无需捕获新密钥 |
| 当前能读取我的企业微信聊天吗？ | 仍未接通；既有企微缓存尚未解密，官方CLI机器人接口也不能证明个人聊天读取 |

本次发现的实现问题：教务根页面出现已知登录过期提示时，原自动换票只接受已经进入门户的页面，先返回 `OPEN_SCHOOL_PORTAL_AFTER_LOGIN`。已补上一次性续接提示的分支，新增4项离线回归；真实学校业务结果来自恢复会话后的直接HTTP查询。首次菜单换票还曾返回 `SCHOOL_HANDOFF_NOT_READY`，检查发现本科页面稍后已加载，再次捕获后验证成功，没有重新扫码。

此表证明实际命令、会话恢复和查询复用；没有把同一上下文中的命令验收称为独立新对话的模型行为测试。私人响应留在本机，未发布到仓库。

## 2026-09-18 OneBot有限试运行

按本人追加要求，安装并试跑第三方Foxerine/ilink-onebot，复用现有ClawBot绑定。真实本机WebSocket调用 `get_version_info`、`get_status`、`get_login_info` 均通过；限时接收进行了10次真实iLink轮询，均为空批次。未收到指定测试文字，未执行任何发送，因此本轮不能记为OneBot消息收发通过。试验进程已自动退出，未创建常驻服务。本人随后明确普通好友/群聊优先，ClawBot仅作补充，不再把补充通道的测试文字作为主线阻碍。

普通微信好友/群聊的候选仍依赖不匹配的Windows版本、UI发送或虚拟桌面，未部署；现有native本地读取继续可用。企微新增检查了需要企业认证/会话存档的OneBot实现及官方长连接机器人SDK，均未证明个人收件箱可读。具体源码、安装故障和能力范围见 [OneBot尝试](skills/wechat-personal/references/workflows/onebot.md)及企微消息契约。

本人完成既有Android企微缓存的一次性只读元信息检查，确认有消息/会话数据库，但相关文件不是明文SQLite；此结果仅证明文件存在，未读取正文，未证明持续收消息。企微目标继续包括历史和后续消息，按本人要求先尝试后续新消息。随后扩展检查OneBots、Satori、Rikka、Akasha-WeChat及Android/iPad候选，发现应用消息范围、Windows依赖、旧客户端版本或缺失协议后端等限制；本轮没有新增普通聊天收发成功记录。

## Android企微通知补充的准备（已暂缓）

本人同意通知转存仅作补充，完整聊天目标保留。已实现标准库有界接收端、手机配置生成、本机归档查询及`wecom_notifications` MCP工具。7项新增测试覆盖实际本机HTTP认证、包名过滤、重复投递、Unicode保真、请求限制及无HTTP归档读取；微信skill共42项测试通过。从已安装skill路径、以`/tmp`为工作目录完成setup、限时监听并退出，真实MCP客户端列出4个工具并调用通知读取，返回空归档。

这些结果仅证明本机实现和MCP连通。手机安装、通知使用权授权、实际企微消息通知投递与读回尚未完成，通知条目仍为`not_connected`；企微完整消息仍未接通。实际凭证和手机配置留在本机，未发布。

本人随后更正手机为原生鸿蒙，并明确手机接入以后再说。Android通知安装及验收待办已撤下，保留已有代码与上述本机验证记录；不把它作为当前验收阻碍要求本人继续操作。OpenHarmony接入是后续方向，尚未实现。

## 2026-09-18 ClawBot发送与个人身份发送

- “ClawBot给我发消息”：从已安装skill、在`/tmp`执行真实发送，API返回消息ID；本人明确确认收到同一条测试文字。机器人向绑定本人的文字发送通过，收消息沿用此前真实入站验收。
- “用个人微信给文件传输助手发一张图片，然后给ClawBot发文字并检查入站”：已获明确授权；本地文件传输助手定位成功，ClawBot会话未查询到，普通OneBot发送后端未接通，两次个人身份发送未执行。本条未通过，不以ClawBot反向发送代替。

skill已区分授权：个人微信身份写请求需要明确或适用的事先授权；ClawBot读写长期授权，无需逐次确认。发送有按操作ID防重及私有尝试记录，命令返回API受理与收件确认分开。没有新增常驻服务。
