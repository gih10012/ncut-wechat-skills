# 可选轻量后台接收服务

状态（2026-09-18）：本人允许以后部署一个轻量常驻接收服务，可运行在本人常开服务器或本机；本轮仅记录方案，常驻实现与部署留待后续。当前没有安装或启动常驻服务。

最新更正：本人手机为原生鸿蒙，手机转发接入已明确暂缓。下文Android来源只保留为旧方案，不再要求安装或验收，也不因允许轻量常驻就继续部署它。后续OpenHarmony手机能力另按实际接口研究；当前继续寻找独立于手机转发的轻量消息后端。

## 与主 skill 的关系

主 skill 按需检索和读取已有数据，不负责常驻轮询、启动服务或安装服务。接收器负责接收、去重和保存；它可独立启停，停止后 skill 仍能读取已有归档。安装或使用 skill 不自动部署后台服务，也不要求 Codex 会话常开。

目前已有的 `notifications serve` 是默认300秒、最多900秒的临时验收入口，尚非独立常驻实现；不要用反复拉起该命令的循环充当正式服务。后续只提取接收所需的小模块，提供独立入口和配置，不让服务依赖主 skill 的执行流程。

## 最小部署范围

- 初始来源为本人 Android 上 SmsForwarder 转发的企微通知；手机需在线、已授权通知使用权，且企微确实产生可见通知。服务器常开无法补齐手机漏发、隐藏内容或历史聊天。
- 采用一个 Python 标准库进程和 SQLite，等待通知推送；不调用模型、不定时扫描全部聊天，不引入 Redis、MySQL、容器或完整机器人平台。
- 服务器与本机二选一部署同一接收器，避免重复接收。服务器方案优先复用已有私有网络或 HTTPS 入口；当前局域网 HTTP 验收地址不能直接作为公网部署方案。
- 主 skill 通过本机归档读取，或按需从本人服务器取回归档；远端读取方式实施时明确，不默认把历史查询接口公开到网络。

同一Wi-Fi不是长期要求：当前临时接收地址只在可互通的局域网内使用；后续若部署可通过互联网访问的认证HTTPS入口，手机使用流量或其他Wi-Fi也可转发，个人电脑无需常开。若采用私有网络，手机需能接入该网络。无论接收器部署在哪里，手机都要能收到企微通知并运行后台转发；断网补发、后台稳定性和手机耗电尚未实测，不保证无遗漏。

企微通知始终标为 `android_notification`，不是完整聊天接通。普通微信好友/群聊 OneBot 收发，以及企微完整新消息和历史读取，仍需各自验证的后端。

## 轻量验收与运维交付

后续实现时以单进程、空闲不轮询为基础，测量并记录空闲及少量连续通知时的 RSS、CPU 和归档增长；建议初始预算为 RSS 不超过64MiB、空闲CPU低于单核1%。这些是拟定预算，尚无常驻实测结果；超出时先说明原因再决定方案。

部署完成后必须在本文件补齐实际服务名称、独立入口、依赖和版本、监听地址、启动/停止/状态/卸载命令，以及是否启用开机启动，并明确告诉本人。当前不提供未经实现的服务单元或部署命令。

凭证、通知正文和数据库保存在部署主机的私有状态目录，配置及数据库权限0600；公开文档只记录目录约定和操作方式。归档保留策略和日志轮转在部署时明确，运行日志不记录凭证或消息正文。现有临时验收数据位于 `~/.local/state/wechat-personal/notification-inbox/<account>/`。

接收器实际投递、去重、重启后继续归档、停止后离线查询、资源占用和卸载均需验证。收到一条实际手机通知只证明通知补充链路；不能据此标记完整企微聊天或普通微信 OneBot 收发完成。

临时手机验收步骤见 [Android企微通知补充](skills/wechat-personal/references/workflows/android-notifications.md)。
