# 验收范围

公众号文章 GET 已实测，输出区分文字层和图片，不把文章读取当作本人收件箱。学校业务会话由配套 ncut-web-api 独立验证。

Linux 微信本地会话列表、名称查询、近期消息正文/发送人/时间已于2026-09-16实测；密钥16/16匹配，195个会话，指定群返回近期消息，实际合并6个WAL提交帧。图片/语音等仅返回类型。微信发送、企微消息、云端完整历史和独立设备登录仍未验收。Linux 当前窗口曾经人工读取，仅限显式窗口诊断。旧伴随端已停止，普通命令不启动容器或加密卷。

未接通是待探索能力的状态，不是停止研究的理由。新会话按 SKILL.md 的发现流程检查当前可用客户端/会话/接口，再维护具体能力。版本进度见仓库 README。
