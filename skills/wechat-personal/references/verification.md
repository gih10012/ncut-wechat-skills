# 验收范围

公众号文章 GET 已实测，输出区分文字层和图片，不把文章读取当作本人收件箱。学校业务会话由配套 ncut-web-api 独立验证。

原生微信/企微消息收发、完整历史和独立设备登录仍未验收。Linux 当前窗口曾经人工读取，仅限显式窗口诊断。旧伴随端已停止，普通命令不启动容器或加密卷。

未接通是待探索能力的状态，不是停止研究的理由。新会话按 SKILL.md 的发现流程检查当前可用客户端/会话/接口，再维护具体能力。版本进度见仓库 README。
