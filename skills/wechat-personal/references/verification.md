# 验收范围

公众号文章 GET 已实测，输出区分文字层和图片，不把文章读取当作本人收件箱。学校业务会话由配套 ncut-web-api 独立验证。

Linux 微信本地会话列表、名称查询、近期消息正文/发送人/时间已于2026-09-16实测；密钥16/16匹配，195个会话，指定群返回近期消息，实际合并6个WAL提交帧。图片/语音等仅返回类型。微信发送、企微消息、云端完整历史和独立设备登录仍未验收。Linux 当前窗口曾经人工读取，仅限显式窗口诊断。旧伴随端已停止，普通命令不启动容器或加密卷。

未接通是待探索能力的状态，不是停止研究的理由。新会话按 SKILL.md 的发现流程检查当前可用客户端/会话/接口，再维护具体能力。版本进度见仓库 README。

2026-09-17 v1：安装第三方 `wechatbot-sdk==0.3.0`，本人扫码后取得真实登录凭证；23:06:29通过其 iLink 接口读取本人发给 ClawBot 的测试文字，并持久化游标。真实响应缺省 `ret/errcode` 已适配；空批次与消息成功分开报告。消息、ID、Token和二维码均仅私存，没有公开到仓库。

同日通过真实 stdio MCP 客户端完成 initialize、tools/list 和三个 tools/call：会话查询返回1个匹配会话；文件传输助手读取5条实际消息；ClawBot复用登录返回合法空增量批次。MCP已注册到本机 Codex，未宣称当前对话已热加载。没有创建常驻服务、自动回复或另一次个人微信登录；原生发送仍未验收。

企微：已观察到本人挂载的Windows目录中有加密会话数据，其中一个 message.db 当日更新；当前Linux没有WXWork进程、Wine或已连接ADB设备。此证据只说明数据存在，不能声称已解密或读到本人消息。用户不接受依赖Windows常驻；后续按非Windows路线研究，不把机器人消息或智能表格MCP当作原生收件箱。

2026-09-18继续验收：从 `/tmp` 使用已安装skill直接读取文件传输助手，返回5条真实本地消息，无需重新登录或捕获密钥。企微已有Android应用私有目录，但当前用户不可读；已准备一次性只读元信息检查，待本人本机授权。没有启动Android容器或改变现有登录。官方WeCom CLI公开的机器人凭证和消息范围也不证明个人聊天可读，详见企微消息契约。

同日追加OneBot尝试：第三方桥接的本机WebSocket元信息接口通过，复用ClawBot凭证的10次真实轮询返回空批次；未收到测试文字、未发送回执，端到端收发待验证。约127MiB的临时进程已在限时结束后退出；没有新增常驻服务。普通好友/群聊与企微个人消息仍未接通。见 [OneBot记录](workflows/onebot.md)。

2026-09-18追加：ClawBot文字发送命令真实返回服务端消息ID，本人确认手机收到，机器人→本人发送通过。个人微信OneBot的图片/文字发送仍未执行、未通过；两条路径单独记录，见[发送契约](capabilities/clawbot/send-text.md)。

2026-09-19：Linux微信已更新为4.1.13，复用原密钥读取文件传输助手和ClawBot历史成功。补充OneBot真实收到本人升级后发送的文字并发出回执，已在Linux微信数据库中独立读回对应新消息；临时桥接退出。原生身份发送依然未接通，详见[OneBot追加验收](workflows/onebot.md)。

2026-09-19媒体验收：本人确认机器人发来的TXT可打开、PNG显示正常；真实入站Excel、JPEG和语音通过iLink收到并下载解密，Excel实际解析、图片打开查看、语音SILK头有效，图片stdio MCP下载与CLI哈希一致。语音没有说话，不宣称转写准确。本人报告原生表情包和公众号卡片均无法从当前客户端发给ClawBot，保持未接通。临时接收已退出，具体范围见[媒体下载](capabilities/clawbot/media.md)及[媒体发送](capabilities/clawbot/send-media.md)。原生个人身份发送仍未完成。
