# 小程序只处理当前业务所需部分

先找该业务是否已有 HTTP 能力、H5 入口或正常请求证据；有则直接复用后端，与小程序界面无关。

只有当前任务必须从小程序取得新身份/数据时才做一次有根据的局部发现。记下实际请求、参数、动态票据/签名来源和结果映射；不把点击路径作为生产子能力，不以源码里的 URL 直接宣称调用成功。

若依赖客户端即时签名/票据且没有已验证取得方式，返回这个具体缺项，完成已有 API 能完成的部分。普通信息任务不启动模拟器、不建设容器或通用小程序驱动。用户另行明确要求攻克这个业务时再扩大调查。
