# 验收范围

已验证本人登录后的课表、空教室、预约日历及预约规则查询；公众号链接读取由配套 wechat-personal 提供。验证依赖实际账号和时间，新的会话应以当前响应为准。

学校 HTTP 查询仅使用 Python 标准库；首次登录使用独立 Chrome 窗口。会话 Cookie/User-Agent 存于本机私有目录，不发布。

2026-09-15：学校服务收藏已通过真实 POST → isf 读回 → POST 恢复 → isf 读回，两次远端写请求成功，原状态已恢复。SSO 换票使用已有会话，无需本人再次认证。对应子能力为 service-favorite。

本地 disabled 任务草稿另已做读写校验；它不是学校订场接口或已启用的调度器。收藏写验证也不等于订场/消息发送成功。版本状态见仓库 README。
# v0 发现与复用补充（2026-09-16—17）

成绩查询从当前教务菜单、iframe、查询表单发现，读取实际学期选项，以本人会话完成只读 POST 并取得非空课程成绩；发现到业务验证约13分钟。封装后再次运行 `grades --account me`，学期和课程结果一致，能力知识文件未改写。正常最新学期有成绩时共两次 HTTP，不重新读菜单或前端 JS。

课表直接调用和跨两组节次的空教室查询已复核。空教室测试发现学校使用 HTTP 200 的“用户没有登录”错误页，现已归类为 AUTH_REQUIRED 并完成原有会话续接。格式校验与离线测试不替代这些业务验证；新对话体验留待本人按仓库 ACCEPTANCE.md 验收。

2026-09-18：夜间503/TLS故障后复查，教务恢复响应但业务会话过期。复用保存的SSO，经教务过期提示和本科菜单换票恢复，无需本人扫码。随后从已安装路径、以 `/tmp` 为工作目录，课表、成绩、跨两个节次的空教室查询均成功；以真实返回的学期ID再次查成绩，课程记录相同，查询前后能力知识文件哈希相同。教务过期提示的自动续接分支已补齐，新增4项离线回归；完整过程与限制见仓库 `ACCEPTANCE.md`。
